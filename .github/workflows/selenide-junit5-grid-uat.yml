name: Run UAT tests (Selenide - full app)

on:
  pull_request:
    branches: [ "master" ]
  push:
    branches: [ "master" ]

permissions:
  contents: write
  pull-requests: write

jobs:
  run-tests:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Start E2E environment
        uses: ./.github/actions/start-e2e-env
        with:
          app-mode: full
          start-selenium-grid: 'true'
          docker-hub-username: ${{ secrets.DOCKER_HUB_USERNAME }}
          docker-hub-token: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Run Selenide UAT tests
        uses: ./.github/actions/selenide-test
        with:
          maven-profile: uat
          test-label: Run Selenide UAT tests (full app)
          artifact-name: allure-results-selenide-uat

  publish-allure:
    needs: run-tests
    if: always() && needs.run-tests.result != 'cancelled' && needs.run-tests.result != 'skipped'
    runs-on: ubuntu-22.04

    concurrency:
      group: allure-pages
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: Download allure results
        uses: actions/download-artifact@v4
        with:
          name: allure-results-selenide-uat
          path: e2e/selenide-junit5-selenium-grid/target/allure-results

      - name: Publish Allure report to GitHub Pages
        uses: ./.github/actions/publish-selenide-allure
        with:
          working-directory: e2e/selenide-junit5-selenium-grid
          destination-dir: selenide-uat
          report-title: Run Selenide UAT tests (full app)
